import projects from "./projects";
import contact from "./contact";
import about from "./about";
import blog from "./blog"

const pages = {
    name: "pages",
    label: "Pages",
    files: [projects, contact, about, blog],
};

export default pages;